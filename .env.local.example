# Upstash Redis — 在 Vercel Dashboard → Storage → Redis (Upstash) 创建后自动注入
# 或在 https://console.upstash.com/ 免费创建
KV_REST_API_URL=
KV_REST_API_TOKEN=

# API Authentication — Skill 端调用上传接口时需要携带的密钥
API_SECRET_KEY=your-secret-key-here
